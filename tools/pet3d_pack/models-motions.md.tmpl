# 加模型 / 换模型 / 加动作 / 换动作

## 动作（.vmd）

1. 把 `.vmd` 丢进 `nyalume\frontends\pet\pet3d\motions\`（放子目录也行，会递归找）。
2. 右键菜单里就会多出对应的一行，**点哪支跳哪支**（当前那支前面带 `▶`）；
   双击左键 = 顺序切下一支。文件多的那几行会被自动截短，菜单整体也会自动缩小，
   不会顶出窗口。
3. 想回默认姿势：菜单 →「回到待机」。
4. 不是所有动作都能直接套：
   - **骨骼要求**（配布作者一般都会写）：上半身2、左右腕捩 / 手捩、肩P、足D / 足IK 等。
     缺次标准骨会抽帧、穿模——这是模型差异，不是程序 bug。
   - **表情名要对得上**（まばたき / 笑い / あ / い / びっくり …），对不上就是"没表情"。
   - **幅度太大的动作会出框**：滚轮缩小、或换更大窗口；也可以在 `启动桌宠.cmd` 里
     调 `--scale`、`--rotate`。
   - **收尾**：舞蹈结束前 1.2 秒会和待机交叉淡出（`END_FADE`，在 `viewer.html` 里），
     所以尾巴上那点别扭的姿势会被淡掉；想改这个时长直接改那个数字。
5. 动作多了菜单会变长（不会出框，但会变小），建议只留常用的几支。

## 模型（.pmx）

1. 把模型目录（含 `.pmx` 和贴图）放进 `assets\` 下，例如 `assets\千咲\`。
2. 如果 `assets\` 下不止一个模型，右键菜单会出现 **「换模型：<下一个>」**，
   点一下就用新模型重启（加载模型是启动期的事，重启最稳，约 3–5 秒）。
3. 也可以直接改 `启动桌宠.cmd` 里的 `set MODEL=` 指到你要的模型目录。
4. 换模型常见的坑：
   - 每个模型的朝向 / 比例不一样，用 `--rotate 180`、`--scale 1.2` 这类参数调；
   - 表情名、骨骼名不同 → 部分互动表情和动作会失效；
   - 贴图 png / tga / bmp 都支持（TGA 解码器已随包带上）。

## 相关文件的职责

| 想改什么 | 改哪儿 |
| --- | --- |
| 动作库 / 模型库 | `motions\` 放 `.vmd`；`assets\` 放模型目录 |
| 默认开哪个模型 / 哪个动作目录 | `启动桌宠.cmd` 里的 `MODEL=`、`VMD=` |
| 光照 / 滤镜组合 | `viewer.html` 里的 `STYLE_PRESETS` |
| 舞蹈收尾过渡时长 | `viewer.html` 里的 `END_FADE` |
| 待机动作 | `motions\idle.vmd`（自然站姿，自制） |

## 给 agent / 脚本用的控制口

桌宠启动后会把控制口的端口写到 `%USERPROFILE%\.nyalume\pet3d_endpoint.json`，
读它就知道往哪发命令（只监听本机，带 `Origin` 头的请求会被 403 挡掉）：

```jsonc
POST http://127.0.0.1:<port>/pet
  {"action":"dance","name":"iris"}    // 换舞（名字可以只写一部分）
  {"action":"idle"}                   // 回待机
  {"action":"say","text":"你好呀~"}    // 气泡说话
  {"action":"face","emotion":"shy"}   // happy/shy/surprise/angry/sad/sleepy/calm/love
  {"action":"react","region":"head"}   // 触发部位表情反应
  {"action":"look","x":0.4,"y":-0.2}  // 看向某处（5 秒内不被鼠标抢走）
  {"action":"style","index":1}        // 换光照+滤镜组合
  {"action":"model","name":"千咲"}     // 换模型（重启式）
  {"action":"quiet","value":true}     // 安静模式
  {"action":"talk_mode","mode":"chatty"} // quiet/reserved/normal/chatty/talkative
  {"action":"proactive","value":false}// 兼容旧接口：切到 quiet

GET  http://127.0.0.1:<port>/pet_state  // 心情/体力/最近互动/在放什么/桌面情况
```

「主动搭话」需要 `LLM_API_KEY`（可配 `LLM_BASE_URL` / `LLM_MODEL`，默认 DeepSeek）；
没配就不会主动搭话，其它功能不受影响。右键菜单可选安静 / 寡言 / 正常 /
健谈 / 话痨，对应关闭 / 45–75 / 20–40 / 10–20 / 3–8 分钟的最短间隔。
「手动安静」单独控制，重启后保留；会议和满载可暂时自动安静，但不会替用户改这个开关。
