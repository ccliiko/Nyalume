# Nyalume 3D 桌宠

真正跑在桌面上的 3D 桌宠：three.js 加载 MMD（.pmx），逐像素透明 + 点击穿透，
支持拖拽、按部位互动、表情、多支 MMD 动作、头发裙摆物理、右键菜单。

## 怎么用

1. 装依赖：双击 **安装依赖.cmd**（需要先装 Python 3.11+）。
2. 放资源：
   - 模型目录（.pmx 和贴图）放到 `assets\` 下，比如 `assets\model\`；
     也可以放好几个，右键菜单会多出「换模型」。
   - 动作：把 .vmd 丢进 `nyalume\frontends\pet\pet3d\motions\`，
     右键菜单里就会列出来，点哪支跳哪支。
   - 细节都写在 **`说明-模型与动作.md`**（含骨骼/表情要求、常见坑）。
3. 双击 **启动桌宠.cmd** 开跑。
   游戏内也要互动就用 **启动桌宠-管理员.cmd**（带反作弊的游戏是高完整性进程，
   不提权收不到它的鼠标输入）。UAC 弹窗点「是」。

## 操作

- 左键点她：按部位戳（头 / 脸 / 胸 / 手 / 裙 / 披风 / 腿）
- 左键按住拖：拖整个人；抓头发 / 衣袖 / 裙摆会带动局部
- 双击左键：下一个动作
- 右键单击：菜单（聊天窗口入口¹ / 动作列表 / 换模型 / 换风格 / 手动安静 /
  主动搭话档位 / 全屏自动降帧 / 退出）
- 右键按住拖：左右旋转　·　滚轮：缩放　·　中键：退出

¹ 聊天窗口需要完整的 Nyalume 主程序；独立桌宠包没有聊天服务。

## 包里有什么

- 桌宠本体：`nyalume\frontends\pet\pet3d\`（`pet3d_win.py` + `viewer.html`）
- 只保留真正用到的 three.js 文件（裁包，MIT License）
- 自带待机动作（自制）
{motions}

模型和部分动作**不在包里**，原因和作者条款见 `CREDITS.md`。
