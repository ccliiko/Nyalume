# Nyalume 隐私说明

生效日期：2026 年 9 月 15 日

本说明适用于 Nyalume 官方桌面发行版。Nyalume 默认是本地优先、单用户应用；当前发行版不包含广告或遥测，也不会由 Nyalume 项目方自动收集使用统计。

## 本地保存的数据

以下数据通常保存在 Nyalume 所在目录或用户选择的工作目录：

- 对话、摘要、便签、提醒、每日卡片状态和运行记录：`agent.db`；
- 模型服务商地址、模型名和 API Key：`.env`；
- 用户导入或生成的文件：`workspace/` 或用户授权的项目目录；
- 本地备份：`backups/`；
- 桌宠、人设和界面设置：本地配置文件。

API Key 当前以明文形式保存在本机 `.env` 中，设置界面不会完整回显密钥。请保护软件目录，不要把填过密钥的副本、备份或日志交给他人。

## 何时会向第三方发送数据

只有在用户启用相应功能时，数据才会发送到用户选择的第三方：

- 对话、相关记忆和检索到的文档片段会发送给配置的模型 API，以生成回答；
- 图片理解会把用户选择的图片发送给配置的视觉模型 API；
- 网页搜索会把查询词发送给搜索服务，浏览器工具会访问用户或模型指定的网站；
- 第三方网站和模型服务商可能记录 IP、请求内容和账号信息，并按照各自隐私政策处理。

请勿向模型或网站发送无权披露的机密、个人信息或受限制数据。Nyalume 项目方无法控制第三方的保存期限、训练政策或跨境传输安排。

## 云同步

当前公开桌面发行版未启用中央账号和云同步。仓库中的 `cloud_service/` 是独立部署组件；只有用户或部署者明确配置并启用后，账号和同步数据才会发送到对应服务器。部署者应另行提供适用于其服务的隐私政策和数据处理说明。

## 删除与保留

用户可以在软件中删除会话、便签、提醒和导入资料，也可以退出软件后删除 `agent.db`、`.env`、`workspace/` 和 `backups/`。删除前请自行保留需要的副本。第三方已经接收的数据应按照对应服务商的工具和政策处理。

## 安全与变更

Nyalume 使用本地权限范围、敏感操作审批、撤销和运行记录降低误操作风险，但任何软件都无法保证绝对安全。本说明发生实质变化时，将更新生效日期并随发行版提供。

隐私问题与安全报告：[GitHub Issues](https://github.com/ccliiko/Nyalume/issues)
